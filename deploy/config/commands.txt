
Other:
    pgrep runserver
    rm /home/ubuntu/MLBStats/backend/nohup.out

    pkill -9 python

Code Update:
    Gunicorn detects file changes. Must restart nginx
        sudo systemctl restart nginx

TODO
ls /etc/nginx/sites-available/
sudo rm /etc/nginx/sites-available/nodomain

Remove SimLink? What happens to files that were simlinks but got removed?
Start gunicorn & nginx

Nginx:
    Move files:
        sudo cp /home/ubuntu/MLBStats/backend/config/nginx/nginx_config /etc/nginx/sites-available/
    SimLink (to backend dir):
        cd /etc/nginx/sites-enabled
        sudo ln -s ../sites-available/nginx_config .
        sudo systemctl restart nginx
    Verify Config:
        sudo service nginx configtest /etc/nginx/sites-available/nginx_config
    Run:
        sudo systemctl start nginx 


Gunicorn:
    Log:
        tail -f /var/log/gunicorn/dev.log
    Run:
        gunicorn -c /home/ubuntu/MLBStats/backend/config/gunicorn/dev.py